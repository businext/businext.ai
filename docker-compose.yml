version: "3.8"

services:
  businext_bot:
    image: businext:latest
    build: .
    restart: unless-stopped
    volumes:
      - ./src:/usr/app/src
      - ./models:/usr/app/models
    command: npm run start:dev
